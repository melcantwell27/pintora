# ═══════════════════════════════════════════════════════════════════
# TEMPLATE ONLY — safe to commit. Do NOT put real secrets here.
#
# Edit frontend/.env.local instead (gitignored). First-time setup copies
# this file for you:  bash scripts/setup.sh
# ═══════════════════════════════════════════════════════════════════

# Origin of the Django API (no trailing /api — schema paths already include it)
# Pintora API runs on 8001 to avoid clashing with other local projects on 8000.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8001
